# Service Request System (FastAPI + PostgreSQL)

Внутренний сервис заявок уровня небольшой компании: сотрудники создают запросы, агенты и админы ведут их по статусам, всё логируется и фильтруется как в мини-Jira.

Проект сделан как пет-проект для портфолио backend-разработчика (Python / FastAPI).

---

## ✨ Основные фичи

- Роли пользователей:
  - **ADMIN** – полные права, может управлять пользователями и заявками
  - **AGENT** – обрабатывает заявки, берет их из очереди или по назначению
  - **EMPLOYEE** – создаёт заявки, может отменять свои

- Жизненный цикл заявки:
  - `NEW → IN_PROGRESS → DONE`
  - `NEW → IN_PROGRESS → CANCELED`
  - Жёсткая проверка допустимых переходов (policy-слой)

- Логирование действий:
  - Таблица `request_logs`
  - Логируется смена статуса, назначение исполнителя, комментарии при отмене
  - IP клиента, user-agent, источник (`API` / бот и т.д.)

- Фильтрация и поиск заявок:
  - фильтр по статусу
  - фильтр по создателю
  - фильтр по исполнителю
  - фильтр по дате создания (from/to)
  - пагинация `limit/offset`

- Работа через API-ключи:
  - каждый пользователь имеет свой API-ключ
  - доступ к методам по ролям

- Готовность к продакшену:
  - PostgreSQL + Alembic миграции
  - отдельный слой сервисов (`services/`)
  - политика доступа (`policies/`)
  - seed-скрипт для первичной инициализации пользователей
  - Docker + docker-compose для быстрого подъёма

---

## 🧱 Технологии

- Python 3.11+
- FastAPI
- SQLAlchemy 2.0 (ORM)
- PostgreSQL
- Alembic (миграции)
- Uvicorn
- Docker, docker-compose

---

## 📂 Структура проекта (упрощённо)

```text
app/
  core/          # конфиг, enums и базовые вещи
  database/      # подключение к БД, Base
  models/        # SQLAlchemy модели (User, ServiceRequest, RequestLog)
  schemas/       # Pydantic-схемы
  services/      # бизнес-логика (requests, users, request_logs)
  policies/      # правила доступа и переходов статусов
  routers/       # FastAPI endpoints
  main.py        # точка входа приложения

alembic/         # миграции БД
seed.py          # скрипт для создания базовых пользователей
docker-compose.yml
Dockerfile
.env.example
